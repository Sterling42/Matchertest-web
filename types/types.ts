export interface TokenData {
  address: string;
  decimals: number;
  logoURI: string;
  name: string;
  symbol: string;
}

export interface TokenInfo {
  name: string;
  symbol: string;
  mintAddress: string;
  decimals: number;
  logoUrl: string;
  verified: boolean;
}