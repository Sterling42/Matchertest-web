export interface TokenData {
  address: string;
  logoURI: string;
  name: string;
  symbol: string;
}

export interface TokenInfo {
  name: string;
  symbol: string;
  mintAddress: string;
  logoUrl: string;
  verified: boolean;
}